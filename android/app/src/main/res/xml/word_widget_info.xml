<?xml version="1.0" encoding="utf-8"?>
<!-- Home screen + lock screen. Opens as a compact 4x1; resize down to 2x1 or up to 4x2 and the
     layout switches to fit. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_desc"
    android:initialLayout="@layout/widget_medium"
    android:previewLayout="@layout/widget_medium"
    android:previewImage="@drawable/tiger_cut"
    android:minWidth="110dp"
    android:minHeight="40dp"
    android:minResizeWidth="40dp"
    android:minResizeHeight="40dp"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="10800000"
    android:widgetCategory="home_screen|keyguard" />
