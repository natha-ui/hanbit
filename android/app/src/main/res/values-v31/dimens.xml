<resources>
    <!-- Match the launcher's own corner radius so the system rounding never clips our text -->
    <dimen name="widget_radius">@android:dimen/system_app_widget_background_radius</dimen>
</resources>
